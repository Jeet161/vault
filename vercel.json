{
  "buildCommand": "npm install && npm run build:shared && npm run build:db && npm run build --workspace=apps/web",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install",
  "framework": "nextjs"
}
